{
  "name": "gh-pull-request-dashboard",
  "version": "2024.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"cd backend && go run .\"",
    "dev": "npm run dev --prefix frontend",
    "backend": "cd backend && go run ."
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
